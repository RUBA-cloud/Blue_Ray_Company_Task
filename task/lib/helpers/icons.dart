//Todo:: Here All Icons used in this Project will be defined in this file

import 'package:flutter/material.dart';
import 'package:task/helpers/colors.dart';

const homeIcon = Icon(Icons.home);
const bagIcon = Icon(
  Icons.local_mall,
  color: colorBlack,
);
const categoryIcon = Icon(
  Icons.menu,
  color: colorBlack,
);
const faviorateIcon = Icon(
  Icons.favorite,
  color: colorBlack,
);

const locationIcon = Icon(
  Icons.location_on,
  color: colorBlack,
);
const accountIcon = Icon(
  Icons.account_box_rounded,
  color: colorBlack,
);
const logoWhite = Icon(
  Icons.holiday_village,
  color: colorWhite,
);
